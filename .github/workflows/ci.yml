name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2   
    - name: Build
      uses: spenserblack/actions-build-emojicode@v0.1.0
      with:
        filename: main.🍇
